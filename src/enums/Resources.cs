namespace Saavn {
    /// <summary>
    /// The different types of resources that can be fetched from Saavn.
    /// </summary>
    public enum ResourceType {
        MUSIC,
        PLAYLIST,
        MEDIA_URL
    }
}
